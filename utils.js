function shuffleTiles() {
  const arr = [...Array(9).keys()]; // [0,1,2,...8]
  for (let i = arr.length - 1; i > 0; i--) {
    const j = Math.floor(Math.random() * (i + 1));
    [arr[i], arr[j]] = [arr[j], arr[i]];
  }
  return arr;
}